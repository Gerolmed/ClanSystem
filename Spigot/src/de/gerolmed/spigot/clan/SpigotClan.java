/* * Copyright (c) 2018. * This content has been created by Gerolmed! Sharing this content without permission is not allowed, as well * as modifying any code parts and claiming ownership! */package de.gerolmed.spigot.clan;import de.gerolmed.lib.clan.ClanUserManager;import net.milkbowl.vault.chat.Chat;import org.bukkit.Bukkit;import org.bukkit.plugin.RegisteredServiceProvider;import org.bukkit.plugin.java.JavaPlugin;public class SpigotClan extends JavaPlugin {    private static Chat chat = null;    @Override    public void onEnable() {        setupChat();        new ClanUserManager();        Bukkit.getPluginManager().registerEvents(new ClanListener(this), this);        Bukkit.getMessenger().registerIncomingPluginChannel(this, "Return", new PluginChannelListener());    }    private boolean setupChat() {        RegisteredServiceProvider<Chat> rsp = getServer().getServicesManager().getRegistration(Chat.class);        chat = rsp.getProvider();        return chat != null;    }    Chat getChat() {        return chat;    }}